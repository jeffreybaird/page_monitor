{
  "name": "page-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "test:e2e": "npm run build && playwright test",
    "verify": "npm run typecheck && npm run lint && npm run format:check && npm test && npm run build && npm run build:firefox",
    "build:firefox": "vite build --mode firefox",
    "dev:firefox": "vite build --mode firefox --watch",
    "test:e2e:firefox": "npm run build:firefox && node scripts/test-firefox.mjs",
    "package:firefox": "npm run build:firefox && web-ext build --source-dir dist-firefox --artifacts-dir artifacts/firefox --overwrite-dest",
    "lint:firefox": "npm run build:firefox && web-ext lint --source-dir dist-firefox --no-config-discovery"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.63.0",
    "@types/chrome": "^0.2.9",
    "@types/node": "^22.20.2",
    "eslint": "^10.10.0",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.70.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.0",
    "web-ext": "^10.6.0"
  }
}
